name: Populate Depot json

on: 
  release:
  workflow_dispatch:

jobs:
  populate:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - uses: meisZWFLZ/pros-depot-from-releases@c721c184530b0c7927a418fdff6d5ec0c34dd860
        with:
          token: ${{ github.token }}
          repo: meiszwflz/LemLib
